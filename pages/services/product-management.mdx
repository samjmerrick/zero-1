import { Page } from '../../components/page'
import Container from '../../components/Container'

export const meta = {
    title: 'Product Management',
}

# Product Management

Good products need excellent Product Managers. 

Product Management is where it all starts.



export default ({ children }) => <Page meta={meta}><Container>{children}</Container></Page>